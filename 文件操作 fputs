#include <stdio.h>
int main()
{
	char arr[1024] = { 0 };

	FILE* pf = fopen("test.txt", "w");
	if (pf == NULL)
	{
		return 0;
	}

	//写文件

	fputs("",pf);
	fputs("",pf);

	fclose(pf);
	pf = NULL;

	return 0;


}
