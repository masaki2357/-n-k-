#include <stdio.h>
#include <ctype.h>
int main()
{
	char ch[] = "AwetgFWEGHN ASGF A W";
	
	int i = 0;
	while (ch[i] != '\0')
	{
		if (isupper(ch[i]))
		{
			ch[i] = tolower(ch[i]);
		}
		
		i++;
	}
	printf("%s\n", ch);

	return 0;
}
